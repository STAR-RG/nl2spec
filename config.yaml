mode: dev

paths:
  dataset_nl: datasets/nl_scenarios.json
  output_dir: outputs/
  schema_ir: nl2spec/core/schemas/ir.schema.json

pipeline:
  generate: true
  compare: true
  export_csv: true
  stats: true

prompting:
  shot_mode: few   # zero | one | few
  k: 2             # usado só se shot_mode == few
  fewshot:
    enabled: true
    dataset_dir: nl2spec/datasets/fewshot
    selection: fixed

llm:
  provider: mock
  mock:
    class: nl2spec.core.llms.mock_llm.MockLLM
