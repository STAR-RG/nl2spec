Context:
The IR represents a Finite State Machine (FSM) runtime monitor.

Use the FSM JSON structure shown in the examples.

Requirements:
- "formalism" must be "fsm".
- "events" must use the structured "body.methods" format.
- "fsm" must use "type", "initial_state", and structured "states".
- "violation" must use "tag" and structured "body.statements".

Constraints:
- Do not invent events, methods, states, transitions, or violation statements.
- Preserve event names consistently across events and FSM transitions.
- Preserve the pointcut structure through "function" and "operation".
- Do not convert the specification into ERE or LTL.
- Follow exactly the structure shown in the examples.

Examples:
{{FEW_SHOT_EXAMPLES}}