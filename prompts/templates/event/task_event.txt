This specification is written in the EVENT formalism.

This specification is called {{SPEC_NAME}}.
{{SIGNATURE_PARAMETERS_TEXT}}

The following events are monitored in this specification:

{{EVENT_BLOCK}}

A violation occurs when one of these monitored events is triggered under the pointcut conditions described above.

The violation message that must be used in the JSON output is:

"{{VIOLATION_SEMANTICS}}"

The generated JSON must include the field:

"domain": "{{DOMAIN}}"

Return ONLY the IR JSON.
Do not include explanations, markdown, comments, or extra text.